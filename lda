if (columnIndex == -1) {
                    logger.error("CSV header '{}' not found for Avro field '{}'.", fieldName, fieldName);
                    throw new IOException("CSV header '" + fieldName + "' not found for Avro field '" + fieldName + "'");
                }
