String schemaString = "message CsvSchema {" +
        String.join(",", List.of(headers).stream()
                .map(header -> "  required binary " + header.replace(" ", "_"))
                .collect(Collectors.joining(";\n", ";\n", ";")) +
        "}";
